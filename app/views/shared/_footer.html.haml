%footer.footer-wrapper.clearfix
  .copyright-wrapper
    .copyright-container.container.d-flex
      .mr-auto.p-2
        &copy; #{Time.zone.now.year}
        &nbsp;
        = '-'
        &nbsp;
        = link_to I18n.t('title'), root_path
      .ml-auto.p-2
        All Rights Reserved

%a#scroll-top{href: "#top"}
  %span.fa.fa-arrow-up

- content_for :javascript do
  :javascript
    (function($){
      loadAssetImages($('header.intro-header, .front'));

      if (!noOpeningAnimation()) {
        $('.cc-window.cc-banner').remove();
      }
    })(jQuery)
