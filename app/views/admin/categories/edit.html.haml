%h2.text-center
  = I18n.t('action.edit')
  = Category.model_name.human

.right
  = link_to I18n.t('action.back'), admin_categories_path
  |
  = link_to I18n.t('action.new'), new_admin_category_path

%div
  = render partial: 'form', locals: { resource: @category, url: admin_category_path(@category) }
