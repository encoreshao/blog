---
import Base from '../../layouts/Base.astro';
import Nav from '../../components/Nav.astro';
import HeroSection from '../../components/HeroSection.astro';
import ProjectsStrip from '../../components/ProjectsStrip.astro';
import TimelineFeed from '../../components/TimelineFeed.astro';
import TagFilter from '../../components/TagFilter.astro';
import { getCollection } from 'astro:content';

const posts = await getCollection('zh', ({ data }) => !data.draft)
  .then(list => list.sort((a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime()));
const allTags = [...new Set(posts.flatMap(p => p.data.tags))].sort();

const site = 'https://blog.icmoc.com';
const description = '邵壮的博客 — 全栈工程师 & AI 研究员。关于 Rails/Ruby 工程实践、Agentic AI 系统、MCP 服务器，以及生产环境软件交付经验的文章。';

const personJsonLd = {
  '@type': 'Person',
  name: '邵壮',
  url: 'https://icmoc.com',
  jobTitle: '全栈工程师 & AI 研究员',
  worksFor: { '@type': 'Organization', name: 'Ekohe' },
  sameAs: ['https://github.com/encoreshao', 'https://icmoc.com'],
};

const websiteJsonLd = JSON.stringify({
  '@context': 'https://schema.org',
  '@type': 'WebSite',
  name: '邵壮的博客',
  url: `${site}/zh/`,
  description,
  inLanguage: 'zh-CN',
  author: personJsonLd,
  publisher: personJsonLd,
});

const blogJsonLd = JSON.stringify({
  '@context': 'https://schema.org',
  '@type': 'Blog',
  name: '邵壮的博客',
  url: `${site}/zh/`,
  inLanguage: 'zh-CN',
  author: personJsonLd,
  blogPost: posts.map(post => ({
    '@type': 'BlogPosting',
    headline: post.data.title,
    url: `${site}/zh/${post.id.replace(/\.md$/, '')}/`,
    datePublished: post.data.date.toISOString(),
    description: post.data.excerpt,
    keywords: post.data.tags.join(', '),
  })),
});
---

<Base
  title="文章"
  lang="zh"
  description={description}
  alternateHref={`${site}/en/`}
>
  <script slot="head" type="application/ld+json" set:html={websiteJsonLd} />
  <script slot="head" type="application/ld+json" set:html={blogJsonLd} />
  <Nav lang="zh" currentPath="/zh/" />
  <main>
    <HeroSection lang="zh" postCount={posts.length} />
    <ProjectsStrip lang="zh" />
    <div class="feed-header">
      <h2 class="feed-title">文章</h2>
      <span class="feed-sub">关于工程、AI 和软件交付的思考</span>
    </div>
    <TagFilter tags={allTags} lang="zh" />
    <TimelineFeed posts={posts} lang="zh" />
  </main>
  <footer>
    © 2026 邵壮 · 上海 ·
    <a href="https://github.com/encoreshao" target="_blank" rel="noopener">GitHub</a> ·
    <a href="https://icmoc.com" target="_blank" rel="noopener">icmoc.com</a>
  </footer>
</Base>

<style>
  main {
    max-width: 1200px;
    margin: 0 auto;
    padding: 56px 40px;
    position: relative;
    z-index: 1;
  }
  @media (max-width: 640px) {
    main { padding: 28px 16px; }
  }
  .feed-header {
    display: flex; align-items: baseline; gap: 12px;
    margin-bottom: 20px;
  }
  .feed-title {
    font-size: 13px; font-weight: 700; color: rgba(var(--fg-rgb),0.5);
    letter-spacing: 2px; text-transform: uppercase;
  }
  .feed-sub { font-size: 12px; color: rgba(var(--fg-rgb),0.2); }
  footer {
    text-align: center; padding: 40px 24px;
    font-size: 12px; color: rgba(var(--fg-rgb),0.2);
    border-top: 1px solid rgba(var(--fg-rgb),0.06);
    position: relative; z-index: 1;
  }
  footer a { color: var(--cyan); text-decoration: none; }
</style>
